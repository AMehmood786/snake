
import os
import sys
VALIDINPUT = '0'
while VALIDINPUT == '0':print = os.popen('clear')
for line1 in p.readlines():print line1<put the logic for reading user input here><put the logic to check for valid user input here and if the user input is valid, then
 assign 1 to VALIDINPUT>
